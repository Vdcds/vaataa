import { Pressable, StyleSheet, Text, View } from 'react-native';
import { colors, radius, spacing, typography } from '@/ui/theme';
export function SegmentedControl<T extends string>({ options, value, onValueChange }: { options: readonly { label: string; value: T }[]; value: T; onValueChange: (value: T) => void }) { return <View accessibilityRole="radiogroup" style={styles.group}>{options.map((option) => { const selected = option.value === value; return <Pressable key={option.value} accessibilityRole="radio" accessibilityState={{ checked: selected }} onPress={() => onValueChange(option.value)} style={[styles.item, selected && styles.active]}><Text style={[styles.label, selected && styles.activeLabel]}>{option.label}</Text></Pressable>; })}</View>; }
const styles = StyleSheet.create({ group: { padding: spacing.xs, gap: spacing.xs, flexDirection: 'row', borderRadius: radius.md, backgroundColor: colors.surfaceMuted }, item: { flex: 1, minHeight: 36, alignItems: 'center', justifyContent: 'center', borderRadius: radius.sm, paddingHorizontal: spacing.sm }, active: { backgroundColor: colors.surface }, label: { ...typography.caption, color: colors.textMuted }, activeLabel: { color: colors.text, fontWeight: '800' } });
